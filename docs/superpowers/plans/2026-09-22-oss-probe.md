# OSS Probe Implementation Plan

> Inline execution: the prescribed superpowers execution skills are unavailable.

**Goal:** distinguish file-stream, SDK and upload-route failures without publishing.
**Architecture:** reuse existing authenticated client; isolated probe module and workflow command.
**Tech Stack:** Node 22, ali-oss, node:https, Vitest.

## Constraints

No release/pointer writes, credential logs, destructive cleanup or auth bypass.

## Steps

- [x] Add probe runner in scripts/diagnose-oss-upload.mjs: 64 KiB/4 MiB PUT matrix and 8 MiB multipart pair, 60s timeout, sanitized JSON report.
- [x] Add client entrypoint generating a unique diagnostic prefix, reusing currentSession.
- [x] Add diagnose workflow choice isolated from publishing; keep main/OIDC checks.
- [x] Test failure continuation, object isolation, transport comparison and report redaction with Vitest; run publisher contract checks.
- [ ] Commit validated demo. Live runner execution requires these changes on main.

Validation: 19 focused tests passed, including real ali-oss file/Buffer multipart
requests received by a loopback HTTP server; typecheck and publisher contract passed.
Loopback HTTP does not validate the real OSS endpoint, TLS, policy or GitHub network.
