# 专家技能列表收敛

已确认：专家技能入口只列出因赛 AI 自带技能，不删除或禁用本机技能，不修改 Core。保留 main 上用户的 LoginView 文案改动。

目录沿用 Core 返回的实际生效路径，仅当技能 ID 属于内置展示表，且路径以 bundled-skills/<相同 ID>/SKILL.md 结束时标记为内置。外部同名技能不进入菜单；无路径不猜测来源；不自行补入被遮蔽的内置技能。此判断为展示分类，不是文件真实性或执行安全认证。

弹层宽度保持 360px，整体最高 360px 并受可用视口空间约束。搜索框高 32px，条目间距收紧，简介最多两行，通过 title 悬停查看完整简介。保留多选、原生 slash 调用、搜索、键盘操作和主题变量。

验收覆盖 macOS/Windows 来源路径、本机同名覆盖、未知技能、无路径、搜索与计数、目录失败及既有元数据降级。自动测试和集成构建通过后由用户在 dev 验收视觉效果。
