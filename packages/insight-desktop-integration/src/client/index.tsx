/** Services required by the empty integration scaffold. */
export const inject: readonly string[] = []

/** Register no browser behavior until the product slots are implemented. */
export function apply(): void {}
